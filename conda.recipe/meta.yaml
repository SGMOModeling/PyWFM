{% set version_match = load_file_regex(
  load_file="../src/pywfm/__init__.py",
  from_recipe_dir=True, 
  regex_pattern='__version__ = "(.+)"') %}
{% set version = version_match[1] %}
{% set build_number = 0 %}

package:
  name: iwfm-pywfm
  version: {{ version }}

source:
  git_url: https://github.com/SGMOModeling/PyWFM.git
  git_tag: master

build:
  noarch: python
  skip: true # [not win]
  number: {{ build_number }}
  string: iwfm{{ iwfm_api_version|replace(".", "_") }}_{{ build_number }}
  script: {{ PYTHON }} -m pip install --no-deps --ignore-installed .
  entry_points:
    - pywfm=pywfm.cli:cli

requirements:
  host:
    - python
    - pip
  
  run:
    - python
    - pandas
    - numpy
    - matplotlib
    - Click
    - requests
    # TODO (0.3.0 release): pin to the iwfmdll build cut from kernel commit
    # 393e02e+ once it ships in the cadwr-sgmo channel. Until then this
    # recipe builds no variants (conda_build_config.yaml has an empty matrix).
    - cadwr-sgmo::iwfmdll=={{ iwfm_api_version }} # [win]

test:
  imports:
    - pywfm

about:
  home: https://github.com/SGMOModeling/PyWFM
  license: MIT
  summary: 'python library to interact with IWFM applications'
  description: |
    python library to expose the functionality of the IWFM API
  dev_url:  https://github.com/SGMOModeling/PyWFM
  doc_url: https://sgmomodeling.github.io/PyWFM/html/index.html
